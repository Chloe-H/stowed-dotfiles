# Useful git  aliases
alias gpom='git push origin master'
alias gpo='git push origin'

# My favorite ls flags
alias lsc='ls --group-directories-first -1A'

# tmux shortcut
alias tmuxc='tmux new -s scratch'
